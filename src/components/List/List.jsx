import { Box, Container } from '@mui/material';
import React from 'react';

const List = () => {
    return (
        <Container>
            <Box>
                list
            </Box>
        </Container>
    );
};

export default List;